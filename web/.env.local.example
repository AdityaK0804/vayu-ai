# Optional: enables the LLM-backed VAYU assistant.
# Without it the assistant still works, answering from the baked data only.
# Free key: https://console.groq.com/keys
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
