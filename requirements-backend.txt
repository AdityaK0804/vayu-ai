# AirSight backend dependencies
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
h3>=3.7
duckdb>=1.0
scikit-learn>=1.3
lightgbm>=4.0
catboost>=1.2
shap>=0.44
joblib>=1.3
xarray>=2023.1
netCDF4>=1.6
rasterio>=1.3
geopandas>=0.14
fastapi>=0.110
uvicorn>=0.29
pyarrow>=14.0

# Optional — plotting
# matplotlib>=3.8

# Optional — Phase 2 STGNN prototype (PyTorch Geometric)
#   pip install torch torch-geometric
# torch>=2.1
# torch-geometric>=2.4

# Optional — Phase 3 multi-agent orchestration (LangGraph)
# Install when wiring /api/v1/agents/analyze:
#   pip install langgraph langchain-core
# langgraph>=0.2
# langchain-core>=0.3
